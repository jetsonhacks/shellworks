# shellworks

Shellworks is the starting point for a minimal terminal assistant that can grow into a more capable local agent environment.

This repository is currently a **work in progress**. The initial goal is to build a small, understandable foundation: a local assistant in the terminal with clear boundaries, simple tools, and room to evolve over time.

More documentation, examples, and code will be added as the project takes shape.
